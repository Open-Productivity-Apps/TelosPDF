// App version shown in About. The build number increments with every
// release build (wired into CI later).
export const APP_VERSION = "26.0.2";
export const APP_BUILD = 1;
// 26.0.2+: updates download themselves during the splash screen; the
// Update button then just restarts into the new version.
export const AUTO_UPDATE_SPLASH = true;
